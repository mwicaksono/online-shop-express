<%- include('../partials/head.ejs', { title: 'All Products' }) %>
    </head>

    <body>
        <ul>
            <% for (const product of products) { %>
                <li>
                    <p>
                        <%= product.name %>
                    </p>
                    <p>
                        <%= product.price %>
                    </p>
                    <p>
                        <%= product.description %>
                    </p>
                </li>
                <% } %>
        </ul>
    </body>

    </html>